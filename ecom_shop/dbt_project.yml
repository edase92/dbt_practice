name: ecom_shop

profile: ecom_shop

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
analysis-paths: ["analysis"] 
clean-targets:
  - "target"
  - "dbt_packages"


models:
  ecom_shop:
    +static_analysis: strict
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts

seeds:
  ecom_shop:
    +schema: seeds
  margins:
    +column_types:
      margin: STRING 
      channel: STRING 